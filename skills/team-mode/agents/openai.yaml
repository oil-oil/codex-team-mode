interface:
  display_name: "Team Mode"
  short_description: "Coordinate focused subagents across substantial tasks"
  default_prompt: "Use $team-mode for this substantial task, starting only the smallest useful set of focused subagents, including none when delegation adds no value."

policy:
  allow_implicit_invocation: true
